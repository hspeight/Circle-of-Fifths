var util = require('../lambda/custom/utils');
import _mainImage from '../lambda/custom/assets';

var date = new Date();

console.log(util.periodOfDay(date.getHours()));

console.log(asset.mainImage);