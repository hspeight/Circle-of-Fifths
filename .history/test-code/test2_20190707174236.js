const util = require('../lambda/custom/utils');

const keys = ['c', 'f', 'b flat', 'e flat', 'a flat', 'd flat', 'g flat',
                'b', 'e', 'a', 'd', 'g'];


newArray = shuffleArray(keys);

console.log(newArray);
